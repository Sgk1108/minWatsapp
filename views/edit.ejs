<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>editYourChat</title>
</head>
<body>
    <h3>Edit Chat</h3>
    <p>This chat was sent from <b><%= chat.from %></b> to <b><%= chat.to %></b></p>
    <form action="/chats/<%= chat._id %>?_method=PUT " method="POST">
        <textarea name="newMsg" s cols="40" rows="5" placeholder="Your old msg:<%=chat.msg%>"></textarea>
        <br><br>
        <button>Submit</button>
    </form>
</body>
</html>